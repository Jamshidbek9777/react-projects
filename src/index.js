import React from 'react';
import ReactDOM from 'react-dom';
import Tally from './index.jsx';

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
      <Tally></Tally>
  </React.StrictMode>
);